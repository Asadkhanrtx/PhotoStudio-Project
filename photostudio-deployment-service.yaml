apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo
  namespace: photostudio-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongo
  template:
    metadata:
      labels:
        app: mongo
    spec:
      containers:
      - name: mongo
        image: mongo:6
        ports:
        - containerPort: 27017
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "300m"
            memory: "256Mi"

---
apiVersion: v1
kind: Service
metadata:
  name: mongo
  namespace: photostudio-ns
spec:
  type: ClusterIP
  selector:
    app: mongo
  ports:
  - port: 27017
    targetPort: 27017

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: photostudio-app
  namespace: photostudio-ns
spec:
  replicas: 2
  selector:
    matchLabels:
      app: photostudio-app
  template:
    metadata:
      labels:
        app: photostudio-app
    spec:
      containers:
      - name: photostudio-app
        image: asadoblivion/photostudio:v1.0.0
        ports:
        - containerPort: 3000

        # Load ConfigMap
        envFrom:
        - configMapRef:
            name: photostudio-config

        # Load Secret
        envFrom:
        - secretRef:
            name: photostudio-secret

        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"

---
apiVersion: v1
kind: Service
metadata:
  name: photostudio-service
  namespace: photostudio-ns
spec:
  type: NodePort
  selector:
    app: photostudio-app
  ports:
  - port: 3000
    targetPort: 3000
